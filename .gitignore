.DS_Store
/target
**/*.rs.bk
/plan

# taskfile
/.task

# goreleaser + zig files
goreleaser-dist/
.intentionally-empty-file.o
*_compiler_family.o
examples/*/target/

# Node/Deno install trees — the example projects commit only their manifest + lockfile.
node_modules/
.deno/
vendor/

# cooldown
.cooldown.lock
.cooldown-versions-*

# hugo documentation site (docs/)
docs/public
docs/resources
docs/.hugo_build.lock
